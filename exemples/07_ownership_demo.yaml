app JournalApp

entity User
    name: String
    email: Email

entity Entry
    content: Text

relation User hasMany Entry

actor User

rule Entry.content required
rule Entry.Update ownedBy User
rule Entry.Delete ownedBy User

workflow ManageEntry for User
    Create Entry
    Read Entry
    Update Entry
    Delete Entry
