#!/usr/bin/env python3
"""Lanceur 'monl' — délègue au CLI orchestrateur (src/cli.py).
Installer : ajouter ce dépôt au PATH, ou 'ln -s $(pwd)/monl ~/.local/bin/monl'."""
import os
import sys

sys.path.insert(0, os.path.join(os.path.dirname(os.path.abspath(__file__)), "src"))
from cli import main  # noqa: E402

main()
