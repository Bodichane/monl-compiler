# Dépendances du compilateur monl et des applications générées.
# Installation : pip install -r requirements.txt
#
# Bornes hautes ajoutées en bêta pour la reproductibilité : on suit les
# correctifs mineurs sans absorber une version majeure potentiellement
# incompatible sans revalidation (voir pyproject.toml, gardé synchronisé).
fastapi>=0.110,<1.0
uvicorn>=0.29,<1.0
lark>=1.1,<2.0
PyJWT>=2.8,<3.0
requests>=2.31,<3.0
pytest>=8.0,<9.0
