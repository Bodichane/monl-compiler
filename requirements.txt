# Dépendances du compilateur monl et des applications générées.
# Installation : pip install -r requirements.txt
#
# Bornes hautes ajoutées en bêta pour la reproductibilité : on suit les
# correctifs mineurs sans absorber une version majeure potentiellement
# incompatible sans revalidation. Ce fichier couvre le runtime de base ; les
# outils de développement et le fournisseur IA sont dans les extras du
# pyproject.toml (`.[dev]` et `.[ai]`).
fastapi>=0.110,<1.0
uvicorn>=0.29,<1.0
lark>=1.1,<2.0
PyJWT>=2.8,<3.0
